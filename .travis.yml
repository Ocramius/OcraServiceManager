sudo: false

language: php

php:
  - 7.0
  - 7.1
  - hhvm
env:
  matrix:
    - DEPENDENCIES="high"
    - DEPENDENCIES="low"

matrix:
  allow_failures:
    - php: hhvm

before_script:
  - composer update --prefer-source

script:
  - ./vendor/bin/phpunit

notifications:
  irc: "irc.freenode.org#zftalk.modules"
