sudo: false

language: php

php:
  - 7.0
  - 7.1
  - hhvm

before_script:
  - composer update --prefer-source

script:
  - ./vendor/bin/phpunit

notifications:
  irc: "irc.freenode.org#zftalk.modules"
